# 04. Decision Log

> **핵심 결정만 기록합니다.** 되돌리기 쉬운 결정은 Issue 안에서 끝내고 여기 오지 않습니다.
> 되돌리기 비싼 결정(범위 · 데이터 · 보안 · 배포)만 여기 남깁니다.

| ID | 결정 질문 | 선택 | 근거 | 영향 Requirement | 재검토 조건 | 결정일 |
| --- | --- | --- | --- | --- | --- | --- |
| D-001 | 프로토타입에서 마이데이터를 실제로 연동할 것인가? | **Mock 데이터** | 핵심 검증 대상은 조합안 이해 UX이며, 마이데이터 연동은 32시간 범위를 초과 | FR-001 · FR-002 · FR-006 · FR-009 | 사용자별 데이터 저장을 검증해야 할 때 | 2026-08-31 |
| **D-002** | **Net Benefit 임계값을 얼마로 둘 것인가?** | **연 50,000원 AND Gross Benefit의 15% — 이중 조건** 🟡 팀 합의 상수 | 실측이 아니다. 2차가 "착수 전 선결 🔴"로 남겼으나 **사업 착수 기준이지 프로토타입 기준이 아니다** — 이 프로토타입이 검증하는 것은 계산 정확도가 아니라 조합안 이해 UX다. 절대값만 쓰면 큰 총혜택에서 미미한 개선에도 변경을 권하고, 상대값만 쓰면 아주 작은 금액 차이에도 신규 발급을 권할 수 있다. 이중 조건이 양쪽 오류를 막고 근거 화면에서 한 줄로 설명된다 | **FR-003** · AC-004 · AC-006 | 실사용자 데이터로 조합안 선택률을 측정할 수 있게 될 때 | 2026-08-31 |
| D-004 | 근거 공개 6항목을 무엇으로 할 것인가? | **실적구간 · 혜택한도 · 연회비 · 제외조건 · 기준일 · 미반영 항목** 🔵 승계 확인 | 2차 목표 `G4`가 6개를 이미 열거했다. 새 결정이 아니라 출처 있는 승계 | FR-005 · AC-002 | 카드사 약관 구조가 바뀌어 항목이 늘 때 | 2026-08-31 |
| D-005 | 계산 기간을 몇 개월로 둘 것인가? | **기준일 + 12개월, "연 n원" 표기** | `User_Flow.png` ②③의 숫자가 이미 연 기준(`혜택한도 월 20,000 × 12` ≈ `추가 혜택 +224,000`, `연회비 −38,000` 안분 없음). 3개월로 잡으면 3개월 혜택 vs 연회비 1년치를 비교하게 되어 게이팅이 거의 항상 "유지"로 떨어진다 | FR-001 · FR-002 · FR-003 | 이벤트성 지출만 다루는 상품으로 범위를 좁힐 때 | 2026-08-31 |
| **D-006** | **앱 내 여정을 경계 고지로 끝낼 것인가, 조합 확정까지 갈 것인가?** | **확정 화면 + 신규 1장 카드사 아웃링크** — 화면 5의 성격을 "경계 고지"에서 **"확정 + 경계"**로 변경 | 북극성 지표가 *"조합안 저장·확정 사용자 ÷ 결과 열람 사용자"*(KB `08_Value_Proposition.md` 5-2절)인데 **확정 액션도, 그것을 담을 엔터티(`OutcomeLog` 폐기)도 없어** 지표가 측정 대상을 갖지 못했다. 차별점 ①(*"실행 설계자"*)도 화면에 근거가 없었다. 아웃링크는 **대행이 아니라 이동**이라 `SCOPE.md` 6절의 제외 사유(직권·대행 권한 없음)에 걸리지 않고, 뱅크샐러드도 발급 연계까지 간다(KB `01_Porters` 5절). 해지는 절차 마찰 민원이 확인된 영역이라(KB `02_가치사슬` 1-5절) **안내만** 남긴다 | **FR-008** · AC-003 · **AC-008**(신설) · UI-008 | 카드사 신청 페이지 정책이 아웃링크를 막을 때, 또는 수익모델(`T32`)이 발급 연계가 아닌 쪽으로 확정될 때 | 2026-08-31 |
| **D-007** | **과거 기준과 미래 기준의 대조를 화면으로 둘 것인가?** | **기준 고르기 화면 + 과거 기준 대조군 화면 신설** — 6화면 → **8화면**(선형 7 + 대조 분기 1). `FR-009` · `UI-009` · `UI-010` · `AC-009` 신설 | 차별점 ②(*"과거가 못 보는 소비"*)를 프로토타입이 **말로만** 주장하고 있었다. `01_Porters` 5절이 뱅크샐러드를 *"미래 소비 반영 ✗ — 최근 1년 소비 내역 기준"*으로 판정해 뒀는데 그 판정을 보여줄 화면이 없어, *"뱅크샐러드랑 뭐가 다른가"*에 답할 장면이 시연에 없었다. 같은 사용자·같은 보유카드에 **기준만 바꿨더니 과거 1위 카드가 미래 기준에서는 해지 대상**이 되는 것을 클릭 한 번으로 보여준다. 대조군은 계산이 없어(Mock 고정 리스트) 구현 비용이 작다 | **FR-009** · AC-009 · UI-009 · UI-010 · `T1`·`T2`·`T5`·`T10` 개정 | 32시간 안에 8화면이 안 나올 때 — 대조군(`[2-A]`)을 먼저 자르고 기준 선택 화면만 남긴다 | 2026-08-31 |
| **D-008** | **과거 패턴으로 미리 채운 값을 언제 미래 계획으로 인정할 것인가?** | **사용자가 `이 계획대로 계산하기`를 누를 때 화면의 전체 값을 미래 계획으로 확정** — 수정 여부와 무관하며 최소 입력 건수·금액 비율은 두지 않음 | 미래 경로는 사용자가 모든 값을 새로 입력하는 흐름이 아니라, 과거 패턴으로 만든 초안을 검토하고 다른 부분만 고치는 흐름이다. 30% 같은 비율은 근거가 없고 큰 미래 지출 1건의 영향도 왜곡한다. 명시적 확인을 기준으로 삼으면 입력 부담을 줄이는 `FR-006`과 미래 입력 없는 결과를 막는 `AC-001`을 함께 지킨다 | **FR-002** · **FR-006** · AC-001 · **AC-010** | 실제 사용에서 제안값을 검토하지 않고 통과하는 비율이 높아 별도 항목 확인 UX가 필요할 때 | 2026-08-31 |
| **D-009** | **"현재 조합 유지" 결론의 원인을 분해해 북극성 분모에서 다르게 처리할 것인가?** | **원인 2종(`임계미달`/`제약과다`) 기록 + 분모는 `임계미달` 유지만 제외 + `제약과다`일 때만 화면 힌트** | 북극성(조합안 선택률) 분모가 *"유지 결론 제외"*인데, 유지의 원인이 두 가지다 — 진짜 임계 미달(제외 정당)과 사용자가 제약을 조여 더 나은 후보가 배제된 경우(`제약과다`). 둘을 구분하지 않으면 후자를 분모에서 빼 선택률이 부풀려지고(지표 오염), 사용자에게 제약 완화 여지를 알릴 기회도 사라진다. 원인 판정에 필요한 정보(임계 판정·제약 완화 가상 후보)는 게이팅 로직에 이미 있어 추가 비용이 작다 | **FR-003** · **AC-011**(신설) · Calculation.hold_reason · PRD 6절 북극성 분모 | 실사용 데이터로 `제약과다` 유지 비율이 무시할 수준으로 확인될 때 — 그때 분해를 로그 전용으로 낮춘다 | 2026-09-01 |
| **D-010** | **약관 기준일 신선도와 6항목 미달을 무효화할 것인가, 고지·부분 제외할 것인가?** | **신선도=경고 고지(결과 유지, 임계 3개월 🟡) · 6항목 미달=카드 단위 후보 제외(전체 거부 아님)** | 프로젝트가 오류·불확실을 숨기지도 과잉 차단하지도 않고 **고지·분리**로 처리해 온 철학(`T7`·`T40`·`T42`)과 일치한다. 신선도 하드 무효화나 6항목 미달 전체 거부는 "유지"가 아닌 "실패" 화면을 늘려 정체성(`T21`)과 충돌한다. 3개월은 실측이 아닌 팀 합의 상수로 `D-002`와 같은 성격이며, Mock은 최신 데이터라 트리거되지 않는다 | **FR-005** · AC-002 · GR3 · GR5 | 실연동에서 약관 갱신 빈도·미달 카드 빈도가 확인돼 임계·처리 방식을 실측 기반으로 재조정할 때 (`OQ-008` 연계) | 2026-09-01 |
| **D-011** | **프로토타입에 최소 백엔드(Prisma + Supabase Postgres + Server Actions)를 넣을 것인가?** | **넣는다** — Mock 데이터의 소재지를 TypeScript Fixture에서 **DB Seed**로 옮기고, 계산은 **Server Action**으로 실행한다. `SCOPE.md` 6절의 *"완전한 백엔드 제외"* 잠금을 **최소 백엔드는 In**으로 개정 | `TEC-01~07`이 이 저장소 Issue로 등록되어 팀이 실연동 직전 단계의 데이터 계층을 검증 대상으로 삼기로 했다. **`D-001`은 유지된다** — 마이데이터·카드사 API 실호출은 여전히 0건이고, 바뀐 것은 Mock의 *저장 위치*와 계산의 *실행 위치*다. **`NFR-002`도 유지된다** — 비밀값은 저장소·클라이언트 번들에 넣지 않고 로컬 `.env`와 Vercel 환경변수로만 주입한다. `TEC-06`이 *"계산 엔진과 UI가 DB에 직접 의존하지 않게"*를 요구해 기존 순수 계산 엔진(`src/domain`)은 무수정으로 재사용된다 | `SCOPE.md` 6절 · `TECH_SPEC.md` 1·2·4절 · `PRD.md` 5·6절 · **`NFR-005`**(신설) · `ADR-001` · `ADR-003` | Seed·Migration·Server Action이 32시간 안에 나오지 않을 때 — Repository 뒤의 Fixture 직접 반환으로 되돌린다. **되돌리기 경로를 코드에 남긴다** | 2026-09-01 |
| **D-012** | **잠금을 푼 범위는 어디까지인가?** | **최소 백엔드까지만** — Prisma Schema · Migration · Seed · Server Actions · 서버 전용 Repository · Vercel/Supabase 환경변수 경계. 그 밖은 **여전히 Out** | `D-011`이 *"백엔드 잠금 해제"*로 읽히면 `cardfit-prd-to-srs` Project의 59건(실 마이데이터 수집·인증·동의 판정·AI SDK 런타임·Supabase RLS·Cron 배치·운영 대시보드·결과 탭·시나리오 3개·페르소나 선택)이 함께 들어와 32시간 범위를 넘고 `T14`·`T17`·`D-001`을 깬다. **그 계획은 v1 로드맵으로 분리**하고 이 저장소의 Issue 70건만 실행한다 | `SCOPE.md` 6절 · `PRD.md` 4절 Won't · `SRS.md` 0절 | 프로토타입 검증 후 v1 착수를 결정할 때 | 2026-09-01 |

---

## 2차에서 승계한 결정

2차 Decision Log 전체를 옮기지 않습니다. **아직 살아 있는 결정만** 위 표에 D-00x로 재번호해서 옮깁니다.

| 2차 결정 | 이번 프로젝트에서 | 처리 |
| --- | --- | --- |
| 핵심 기능 = F-04 Net Benefit 게이팅 | 그대로 승계 | → `docs/SCOPE.md` 5절 |
| 개선축 2개 분리 (시간축 / 조합축) | 그대로 승계 | → `01-product-context.md` |
| MoSCoW 프레임 채택 (RICE 아님) | 승계, 근거 유지 | → `docs/PRD.md` |
| TAM-SAM-SOM 포함관계 논쟁 | **종결** — 최종 정의만 남기고 이력은 옮기지 않음 | 2차 저장소 링크로 대체 |

원본: [`jennie-brain/team-project_2nd/decision-log/decision-log.md`](https://github.com/jennie-brain/team-project_2nd/blob/main/decision-log/decision-log.md)
