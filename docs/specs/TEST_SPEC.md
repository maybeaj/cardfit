# Test Spec — CardFit

> 상태: 🟢 구현 기준본 · 단위: Vitest · E2E: Playwright

## 1. 검증 원칙

계산 정확성은 정답셋 비교로, UX 경계는 사용자 흐름으로 검증한다. 실연동·사용자 조사·선택률은 이 프로토타입의 테스트 통과 주장에 포함하지 않는다. 마이데이터·AI 연동은 검증 대상이 아니다.

## 2. P0 인수 테스트

| Test ID | 요구사항 | 검증 |
| --- | --- | --- |
| `T-AC001` | AC-001 | 전부 삭제/0원일 때 다음 비활성, 결과 객체 0건 |
| `T-AC002` | AC-002 | 근거 6항목 표시, 하나 제거 시 결과 거부, 미반영 상한은 차액 미합산 |
| `T-AC003` | AC-003 | 해지 버튼 0개, 실행 경계 문구 노출 |
| `T-AC004` | AC-004 | `42,000원`, `31,000원` 케이스 모두 유지 반환 |
| `T-AC005` | AC-005 | 모든 카드에 신규·유지·정리 중 하나만 표시, 임계 미달 시 신규·정리 0건 |
| `T-AC006` | AC-006 | 변경/유지의 손실 방향과 비교 기준선이 정확함 |
| `T-AC007` | AC-007 | 이벤트 선택 화면 0개, 감소 입력 정상 처리 |
| `T-AC008` | AC-008 | 근거에서만 확정 진입, 신규 링크 최대 1개, 3블록 표시 |
| `T-AC010` | AC-010 | 미수정 제안값 확인으로 계산 가능, 출처/확인 문구 표시 |
| `T-AC011` | AC-011 | 랜딩·마이데이터 이용 동의 문구, 예시 데이터 안내, 실제 인증 처리·페르소나 선택 미구현 |
| `T-AC012` | AC-012 | 현재 금액에 과거 기준 라벨 2개, 최종 조합 판정 0건 |

## 3. 계산 단위 테스트

- 임계 경계: `49,999/50,000원`, Gross의 `14.99/15%`를 각각 검증한다.
- 같은 입력을 3회 실행해 결과 객체의 깊은 동등성을 확인한다.
- 동률 후보가 정해진 순서로 선택되는지 확인한다.
- 배분 합과 입력 총액의 차이가 1원 이하인지 확인한다.
- 연회비를 안분하지 않으며 7개월 이후 발급 고지가 생성되는지 확인한다.
- 출처 없는 `unmodeled_bound`가 결과에서 제외되는지 확인한다.

## 4. E2E·반응형

- `change_case`와 `maintain_case` 모두 온보딩부터 확정까지 같은 흐름으로 완주한다.
- 402×874와 1440×900에서 가로 스크롤 없이 핵심 CTA와 결론이 보인다.
- 외부 링크를 새 탭으로 연 뒤 돌아오면 입력과 확정 조합이 유지된다.
- 금지어와 Mock 오인 문구를 정적 스캔한다.

## 5. 서버·데이터 경계 테스트

- Prisma migration을 로컬 Supabase CLI 환경에 적용하고, 12개월 Mock Seed가 재현 가능하게 적재되는지 확인한다.
- Server Action의 정상 응답·입력 오류·DB 오류를 검증한다. DB 또는 규칙 엔진 오류가 성공 결과로 표시되어서는 안 된다.
- 실제 MyData·카드사 데이터 API·AI/Gemini 호출이 발생하지 않는지 테스트 환경에서 확인한다.
- 결론 도달시간, 핵심 `ClientEvent`, AC/Guardrail 결과를 검증 증거로 남긴다(`NFR-004`).

## 6. 완료 판정

P0 테스트와 서버·데이터 경계 테스트 전부 통과, 알려진 콘솔 오류 0건, 비밀키·개인정보 스캔 0건이어야 프로토타입을 완료로 표시한다. 사용성·선택률은 관찰하지 않았다고 별도로 고지한다.
