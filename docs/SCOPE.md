# 범위 잠금 (Scope Lock)

> **상태: 🟢 잠금 — 2026-09-01 10:20 KST · CardFit 프로젝트팀 공동 승인**
> 잠금 이후 "이것도 넣을까요?"의 답은 전부 이 문서입니다. 바꾸려면 Decision Log에 재검토 사유를 남깁니다.

이 값들은 새로 정하는 것이 아니라 **2차 역기획 프로젝트의 결론을 승계**한 것입니다.
출처: `docs/archive/2nd-project/01_서비스_역기획서.pdf`, 2차 워크북 09번 문서.

---

## 1. 한 문장 목표

**결혼·이사처럼 예정된 고액 지출을 앞둔 다장 카드 사용자가, 미래 지출 계획을 기준으로 복잡한 카드 조건과 전환비용을 비교하고, 현재 카드를 유지할지 더 유리한 조합으로 바꿀지와 결제 계획을 근거와 함께 5분 안에 결정하도록 돕는다.**

## 2. 핵심 사용자 — 1개

| 항목 | 내용 |
| --- | --- |
| 정의 | 혼인 등 대형 지출 이벤트를 앞두고 **미래 지출이 과거와 달라지는** 다장 보유 사용자 |
| 근거 | 2차 SOM = 혼인 Beachhead (실행채널 + 연간 플로우 기준) |
| 이번 데모에서 제외 | 카드 1장만 쓰는 사용자, 법인카드 사용자 |

## 3. 핵심 사용자 흐름 — 1개 (Happy Path)

```
[0] 랜딩/웰컴   `카드핏.png` 로고 · 서비스 한 줄 설명 · 모바일 우선
              ↓  "카드조합 추천받기"
[1] 마이데이터 이용 동의  보유 카드·최근 소비 이용 목적 안내 · 예시 데이터 안내
              ↓  "마이데이터 이용 동의하기"
[2] 현재 카드와 혜택 확인  보유 카드·현재 혜택·최근 소비 기준 확인
              ↓  "앞으로 쓸 돈 반영하기"
[3] 입력     "앞으로 쓸 돈을 알려주세요" · 초기값 자동 제안 (F-11)
             미래지출 입력·수정 (F-01)
              ↓
[4] 변경 조건  최대 카드 수 · 신규카드 비교 여부
              ↓
[5] 결론+배분  확인된 미래지출 기준 혜택 계산 (F-03)
             조합 최적화 · Net Benefit 게이팅 (F-04)   ← 이 계산이 제품의 전부
             결제수단 배분 (F-05)                      ← 이 화면의 본문
              ↓
[6] 근거     계산 근거 공개 (F-06) — 6항목 + 미반영 항목
              ↓
[7] 확정+경계  조합 확정 (T25) — 확정한 조합 요약 · 카드별 다음 행동
             실행 경계 고지 (F-12) — 신청·해지는 카드사에서
             신규 1장만 카드사 아웃링크 · 해지는 안내만
              ↑
             (수정 루프) 결론 → 입력 되돌아가기 · 재계산
```

> **2026-08-31 정정 이력** — 이 흐름은 `F-01`부터 시작해 **Mock 로드와 F-11(초기값 제안)이 빠져 있었습니다.**
> `docs/ux/README.md`가 이 절을 정본으로 지목하고 있어, 그대로 따르면 **빈 입력 폼**을 만들게 됩니다.
> CJM 14절이 온보딩 이탈(조민아형)의 개선안으로 지목한 것이 정확히 F-11이라 누락이 치명적이었습니다.
> 라우트 경계와 F-11 위치는 `docs/grill/GRILL_LEDGER.md` `T1`·`T3`에서 확정했습니다.

## 4. 중요 예외 — 1~2개

| 예외 | 왜 중요한가 | 상태 |
| --- | --- | --- |
| **임계 미달 → "현재 조합 유지" 반환** | 바꿀 가치가 없을 때 바꾸라고 하면 서비스 신뢰가 무너집니다 (G3). **Error가 아니라 정상 결과 화면으로 설계합니다** | 🟢 확정 — `AC-004` · `UI-005` |
| **Empty — 미래지출 입력 0건** | 과거만으로 계산한 결과를 결과로 내놓지 않는다는 선언입니다 (G2). 이 예외가 없으면 "미래 기준"이 말뿐이 됩니다 | 🟢 확정 — `AC-001` |

→ 화면 상태 3종(Loading · Empty · 임계 미달)은 [`SRS.md`](SRS.md) 7절이 정본입니다.

## 5. 기능 범위

| 구분 | 내용 |
| --- | --- |
| **핵심 기능 1개** | **F-04 조합 최적화 — Net Benefit 게이팅.** 나머지는 이 계산이 성립하기 위한 전후 단계 |
| **보조 기능 2개** | F-06 계산 근거 공개 · F-11 초기값 자동 제안 |
| **축 주의** | 위 "핵심/보조"는 **시연 비중**의 축입니다. **MoSCoW에서는 F-06·F-11 모두 Must**입니다 — F-11은 *"기능 추가가 아니라 MVP 성립 조건"*(2차 정본 3절). → `PRD.md` 5절 · `SRS.md` 0절 |
| **전체 요구사항** | 활성 FR 8개 · UI 10개 (→ `docs/SRS.md`) — 온보딩·예시 데이터 진입 UI 2개 포함, `T35`의 기준 선택·대조군은 철회 |
| **프로토타입 구현 Issue** | Epic 9건 + 압축 후 원자 Task 49건 + 기술 제약 보완 Task 7건 = 총 56건 (→ `docs/tasks/ISSUE_MAP.md`) |

## 5-1. 구현 기술 제약

| ID | 제약 | v0.1 적용 |
|---|---|---|
| C-TEC-001 | 모든 서비스는 Next.js App Router 단일 풀스택으로 구현 | 적용 |
| C-TEC-002 | 서버 로직은 Server Actions를 사용하고 별도 백엔드 서버는 만들지 않음 | 적용 |
| C-TEC-003 | Prisma + 로컬 Supabase CLI로 개발하고 배포 시 Supabase PostgreSQL 사용 | 적용 |
| C-TEC-004 | Tailwind CSS + shadcn/ui 사용 | 적용 |
| C-TEC-005 | Vercel AI SDK | 기각 — AI 미사용 |
| C-TEC-006 | Google Gemini API | 기각 — AI 미사용 |
| C-TEC-007 | Vercel Git Push 자동 배포 | 적용 |

`C-TEC-003`은 중복 정의하지 않고 위 문장을 정본으로 사용한다.

## 6. 명시적 제외 (Out of Scope)

| 제외 항목 | 이유 |
| --- | --- |
| 해지 · 전환 실행 **대행** · 상담 안내 | 마이데이터 기반 서비스에 직권·대행 권한이 없음 (2차 결론).<br>**신규 발급 1장의 카드사 아웃링크는 제외 항목이 아닙니다** — 대행이 아니라 이동입니다 (`T25`). 해지는 안내만 남깁니다 |
| **F-02 마이데이터 실연동** | 프로토타입에서는 **Mock 데이터**. 실제 서비스와 같은 `마이데이터 이용 동의하기` 화면을 두고, 본문에서 예시 데이터 이용 사실을 안내한다. 실제 동의·연결 처리는 구현하지 않음 (→ D-001) |
| 실제 마이데이터·카드사 데이터 API · 실사용자 인증 · 민감정보 처리 | 프로토타입 범위 밖. 고정 Mock Seed를 사용 |
| 완전한 운영 백엔드 · 관리자 기능 · 고가용성 인프라 | 범위 밖. 단, Prisma·Supabase와 Server Actions의 최소 서버 계층은 포함 |
| **카드 Rule 갱신 운영** — 수집·검수·갱신 주체(RACI)·SLA·오배포 실시간 감지 (`T29`·Q11·S15) | 실연동 사업 착수 기준이지 프로토타입 기준이 아님. Mock은 고정 Rule이다 → `OQ-008`. **Mock→실연동 전환 시 먼저 깨질 상수 목록**은 `knowledge-base/03` 참조 (`D-001` 재검토 조건 구체화, `T29`·S20) |
| **결제 취소·부분 취소 예외 처리** (`T29`·Q12) | 실연동 범위. 프로토타입의 Mock 제외 항목은 별도 검증된 공식 약관 근거가 생긴 뒤 확정한다 |
| **아웃링크 404 실시간 감지·전환(Attribution) 추적** (`T30`·Q13·Q14) | 실연동 범위 → `OQ-011`. 프로토타입은 공식 홈 링크 이동만, 클릭 이벤트는 클라이언트 로깅으로 계측 (`T30`) |
| **면책 약관·CS 프로세스·마이데이터 사업자 자격·카드사 약관 표시 권리** (`T31`·Q17·Q18·Q29·H26) | 사업 착수·법률 검토 범위 → `OQ` 신규. 프로토타입은 예시·참고용 면책 문구만 노출 (`T31`) |

## 6-1. 승인 전에 값을 정해야 하는 것 — ✅ **0건 (전부 확정)**

2차가 "착수 전 선결"로 남긴 항목 중 **이번 구현을 직접 막는 것**입니다.
실측이 아니라 **팀 합의 상수로 고정하고 성격을 표기**합니다. 사용자 행동 지표와 계산 상수를 같은 근거로 취급하지 않습니다.

**2026-08-31 정본 대조 결과 2건은 이미 문서에 값이 있었고, 남은 1건(`D-002`)은 `T16`에서 확정했습니다.**

| # | 항목 | 막는 것 | 값 | 성격 | 결정 ID |
| :---: | --- | --- | --- | :---: | :---: |
| **1** | **Net Benefit 임계값** (2차 D2) | ~~FR-003 게이팅을 코드로 쓸 수 없음~~ | **연 50,000원 AND Gross의 15%** (이중 조건) | 🟡 **팀 합의 상수 — 확정** | **D-002** |
| 3 | 근거 공개 6항목 구성 (A-05) | FR-005의 "6항목 미달 시 거부" 판정 | **실적구간 · 혜택한도 · 연회비 · 제외조건 · 기준일 · 미반영 항목** | 🔵 승계 확인 — 정본 1-1절 **G4**에 열거됨 | D-004 |

**D-002 최종 형태**

```
Net Benefit = Gross Benefit − (연회비 + 실적 재적립 손실 + 발급 대기 비용)
                               └── 전환비용 3항목 (2차 확정) ──┘

변경 제안 조건 —  Net Benefit ≥ 50,000원  AND  Net Benefit ≥ Gross Benefit × 15%
그 외              →  "현재 조합 유지"
```

**2026-08-31 확정** (`T16` · `D-002`) — 실측이 아니라 **프로토타입 계산을 위한 팀 합의 상수 🟡**입니다.
절대값만 쓰면 Gross 90만원에서 6.7% 개선에 변경을 권하게 되고, 상대값만 쓰면 연 3천원 차이로 신규 발급을 권해
작은 차이만으로 신규 발급을 권하는 과잉 제안을 만들 수 있습니다. 이중 조건이 양쪽 구멍을 막습니다.

→ 판정 근거는 [`REUSE_MAP.md`](REUSE_MAP.md) 1절 · 대조 이력은 [`SRS.md`](SRS.md) FR-002·FR-003·FR-005

## 7. 기술 학습 주제 — 최대 3개

→ `docs/TECH_LEARNING_PLAN.md`

---

## 승인

| 항목 | 값 |
| --- | --- |
| 승인자 | **CardFit 프로젝트팀 공동 승인** |
| 승인 시각 | **2026-09-01 10:20 KST** |
| 재검토 조건 | Day 3 Build Cycle 1에서 F-04 계산이 시간 내 불가능하다고 판명될 때 |
