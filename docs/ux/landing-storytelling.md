# CardFit 랜딩페이지 콘텐츠 계획서

> 상태: 🟡 초안 · 2026-09-02 (v0.4 프로토타입 기준으로 갱신)
> 상위 문서: `docs/PRD.md` · `docs/ux/README.md` 1절 · `docs/knowledge-base/raw/CardFit_Deck_10min_v2-2.md`(마스터덱 18장) · `docs/prototype/cardfit-prd-srs-v0.4.html`(앱 화면 목업 v0.4)
> 소유 레인: UX & Build (`docs/ux/**`)

이 문서는 실제 웹페이지(`/`)에 뜨는 **순서 그대로** 섹션마다 무슨 내용이 들어가는지 정리한다.
확정된 카피는 출처를 밝히고, 우리 자료에 없는 부분은 지어내지 않고 **미정**으로 남긴다.

## 지켜야 할 것

- 랜딩은 앱 데모의 파생물이다 — 화면 설명·금액은 `/app` 계산 결과와 어긋나면 안 되고, `src/content/copy.ts`·`src/domain/calc.ts`의 값을 그대로 쓴다. 랜딩에만 있는 숫자를 새로 만들지 않는다.
- 금지어: `총혜택`, `최대혜택`, `놓쳤어요`, `손해보고 있어요`.
- 페르소나(이서연·신은서·조민아)는 마스터덱에 **모의 응답**이라고 명시돼 있다 — 실제 고객 후기처럼 인용하지 않는다.
- 북극성 40%, 계산오류율 등 내부 검증 지표는 팀 자료용이다 — 고객 대상 화면에 노출하지 않는다.
- `docs/prototype/cardfit-prd-srs-v0.4.html`은 **앱 화면(iPhone 목업) 기준으로 대충 구상한 탐색용 프로토타입**이다. 랜딩(웹페이지)에 그대로 옮기지 않고, 이 문서가 웹 섹션 형태로 번역한 내용만 반영한다. 문구가 `CLAUDE.md` 확정 제약과 어긋나면 **`CLAUDE.md`가 우선**한다(아래 12절 참고).

---

## 1. 상단바

| 항목 | 내용 |
| --- | --- |
| 로고 | CardFit 워드마크(가로형, 이번에 받은 로고 일러스트 반영) |
| 버튼 | 이용 흐름 (페이지 내 앵커) — 실제 배포 시 `/app`으로 교체 |

---

## 2. 히어로

| 항목 | 내용 | 출처 |
| --- | --- | --- |
| 배지 문구 | FUTURE SPEND, CLEARER CHOICES | `docs/prototype/cardfit-prd-srs-v0.4.html` s0 화면 |
| 헤드라인 | 앞으로 쓸 돈까지 반영해 카드 조합을 다시 계산합니다 | v0.4 s0 (채택 — 아래 "결정 사항" 참고) |
| 서브 카피 | 현재 카드 혜택만 보는 대신, 앞으로의 지출 계획까지 확인해 내게 맞는 결제 조합을 찾아보세요 | v0.4 s0 |
| 1차 버튼 | 카드조합 추천받기 → `/app` | v0.4 s0 CTA 문구 채택 |
| 보조 문구 | 예시 데이터로 동작하는 프로토타입 · 실제 금융정보를 연결하지 않습니다 | v0.4 s0 배지 + s1 안내 문구를 합침 |
| 이전 대안(참고용) | 헤드라인 "과거 분석을 넘어, 앞으로 쓸 돈으로 카드 조합을 다시 계산합니다" · CTA "앱 데모 시연하기" | 마스터덱 슬라이드 1 / `src/app/page.tsx` 기존 배포본 — 이번 초안에서는 v0.4 쪽을 채택하고 이 문구는 대안으로만 남김 |
| 히어로 비주얼 | **미정** — 이번 초안은 숫자 없이 "1분이면 충분해요" 안내 카드 + 미래지출 입력 예시(가전·가구 840만원 등)로 대체했다(v0.4 s0에 숫자 배지가 없어서 따름). 스크린샷·일러스트로 바꿀지는 팀 확인 필요 | v0.4 s0 |

---

## 3. 문제 제기 (신규 제안 — 현재 배포 페이지엔 없음)

마스터덱은 히어로 다음에 바로 "기존 vs CardFit 대비"로 들어가는데, 그 앞에 **문제 자체의 대비 한 줄**을 넣으면 왜 다른지가 먼저 와닿는다.

| 항목 | 내용 | 출처 |
| --- | --- | --- |
| 왼쪽(기존) | 지난 1년, 이미 쓴 돈 → "어떤 카드가 좋았나?" | 마스터덱 슬라이드 9 |
| 오른쪽(CardFit) | 앞으로 3개월, 곧 쓸 돈 → "지금 무엇을 준비해야 하나?" | 마스터덱 슬라이드 9 |
| 채택 여부 | **미정** — 이 섹션 자체를 넣을지는 팀 확인 필요. 넣는다면 4번 "차이 비교" 섹션 바로 앞에 붙는다 | — |

---

## 4. 차이 비교 (기존 추천 vs CardFit)

| 항목 | 내용 | 출처 |
| --- | --- | --- |
| 기존 추천 | 지난 1년 소비 → 카드 한 장 비교 → 발급 페이지 연결 | `src/app/page.tsx` 현행 (마스터덱 슬라이드 5와 동일 구조) |
| CardFit | 현재 소비 확인 → 앞으로 쓸 돈 입력 → 카드 조합 재계산 → 근거 확인 → 카드사 연결 | 현행 |
| 캡션 | 차이는 발급 연결이 아니라, 그 전에 '미래 입력'과 '조합 계산'을 거친다는 점입니다 | 현행 (마스터덱 슬라이드 5 원문과 동일) |

---

## 5. 고객 가치 (3종)

| 제목 | 내용 | 태그 | 출처 |
| --- | --- | --- | --- |
| 더 큰 절약 | 앞으로 12개월 지출까지 계산해 더 유리한 카드 조합을 찾습니다 | 미래지출 + 조합 계산 | 현행 / 마스터덱 슬라이드 2·12 |
| 확신 있는 선택 | 적용된 실적구간·한도·연회비·제외조건을 그대로 펼쳐 봅니다 | 근거 6항목 공개 | 현행 |
| 쉬운 결정 | 복잡한 조건은 규칙 엔진이 계산하고, 사용자는 결정에 집중합니다 | 결론 우선 UI | 현행 |

---

## 6. 이용 흐름 (7단계 — v0.4 mermaid 흐름 채택)

| 순서 | 단계 | 출처 |
| --- | --- | --- |
| 1 | 마이데이터 이용 동의 | v0.4 mermaid: `랜딩/웰컴 → 마이데이터 이용 동의` |
| 2 | 현재 카드와 혜택 확인 | v0.4 mermaid |
| 3 | 앞으로 쓸 돈 확인 | v0.4 mermaid |
| 4 | 변경 조건 설정 | v0.4 mermaid |
| 5 | 바꿀 가치 판단 (Net Benefit 게이트) | v0.4 mermaid `F{Net Benefit 게이트}` |
| 6 | 근거 6가지 확인 | v0.4 mermaid |
| 7 | 조합 확정 | v0.4 mermaid |

캡션: 바꿀 가치가 없으면 **"현재 조합 유지"**도 정상 결과입니다. 실패 화면이 아닙니다. (`docs/ux/README.md` 2-5절과 일치)

⚠️ 1단계 "마이데이터 이용 동의"는 v0.4 화면(s1) 문구를 그대로 옮긴 것이다. `CLAUDE.md`는 실제 동의를 구현하지 않고 CTA를 "예시 데이터 연결하기"로 고정했으므로, **실제 `/app` 구현과 랜딩 문구는 반드시 `CLAUDE.md` 쪽을 따라야 한다** — 이 표의 1단계 라벨은 v0.4 원문 인용이지 채택 문구가 아니다.

---

## 7. 화면 미리보기 (4종)

| 번호 | 제목 | 내용 | 근거 화면 | 출처 |
| --- | --- | --- | --- | --- |
| ① | 앞으로 쓸 돈을 확인해주세요 | 최근 소비 패턴으로 미리 채웠습니다. 달라질 항목만 수정해 주세요 | `/app/plan` (FR-001) | v0.4 s3 |
| ② | 얼마나 더 유리한가 | 바꿀 가치가 충분할 때만 변경을 제안하고, 아니면 지금 조합을 유지해요 | `/app/result` (FR-003) | v0.4 s5 |
| ③ | 이 결과가 나온 이유 | 결론마다 6개 근거를 확인합니다. 미반영 항목은 결론 차액에 합산하지 않습니다 | `/app/evidence` (FR-005) | v0.4 s6 |
| ④ | 확정과 실행 경계 | 신청·해지는 카드사에서 직접 진행하셔야 합니다. CardFit은 계산 결과와 안내만 제공합니다 | `/app/confirm` (FR-008) | v0.4 s7 |
| 비주얼 | **미정** — 실제 화면 스크린샷을 캡처해 넣을지, 지금처럼 텍스트 카드로만 둘지 결정된 바 없음. `docs/prototype/cardfit-prd-srs-v0.4.html`이 8화면짜리 iPhone 목업(온보딩→마이데이터 동의→현재 확인→미래지출→변경조건→결과→근거→확정)을 갖고 있어, 캡처를 쓰기로 하면 이 파일이 1차 후보 소스다 | | | |

---

## 8. 계산 신뢰 / 원칙

| 항목 | 내용 | 출처 |
| --- | --- | --- |
| 제목 | 금액과 설명 모두 카드 혜택 규칙과 고정 문구로 처리합니다. 생성형 AI를 쓰지 않습니다 | `CALC_NOTICE.engine`(`src/content/copy.ts`) |
| 계산 단계 4개 | 확인한 12개월 계획 펼치기 / 카드별 실적·한도·제외조건 적용 / 조합 후보 만들기(사용 2장·신규 1장 이내) / 순혜택 임계 판정 | `CALC_NOTICE.steps` |
| 변경 권하는 기준 | 순혜택 ≥ 50,000원 그리고 추가 혜택의 15% 이상 | `NET_BENEFIT_FLOOR`·`NET_BENEFIT_RATIO`(`src/domain/calc.ts`) |
| 결론 표기 | 사용 카드 2장 · 신규 1장 이내에서의 최선 | `CONCLUSION_COPY.boundedOptimum` |
| 각주 | Net Benefit = Gross Benefit − 연회비 − 실적 재적립 손실 − 발급 대기 비용. 임계값은 실측이 아니라 과잉 추천을 막기 위한 팀 합의 상수입니다 | 공식 문구는 현행, 계산식 문장은 v0.4 결과 화면(s5) 표기를 채택 |

---

## 9. 실행 경계

| 항목 | 내용 | 출처 |
| --- | --- | --- |
| 제목 | CardFit의 실행 경계 | `BOUNDARY_COPY.headline` |
| 본문 | 신청·해지는 카드사에서 직접 진행하셔야 합니다 — 새 카드 신청은 카드사 공식 페이지로 이동만 제공하고, 신청을 대신 진행하지 않습니다 | `BOUNDARY_COPY.direct` + `outlinkNote` |
| 각주 | ※ 수치는 예시이며 실제 약관과 다를 수 있습니다 · 실제 결제·인증·민감정보를 다루지 않습니다 | `DATA_NOTICE.sampleFootnote` |

---

## 10. 마무리 CTA

| 항목 | 내용 |
| --- | --- |
| 버튼 | 카드조합 추천받기 → `/app` (v0.4 s0 CTA 채택) |
| 실제 배포 URL | **미정** — `README.md`에 "Deployed Product: TODO — vercel --prod 실행 후 URL 기입"으로 남아 있음 |

---

## 11. 푸터

**미정** — 현재 페이지에 별도 푸터 섹션 없음. 필요 여부(팀 소개, 저장소 링크, 문의처 등) 팀 확인 필요.

---

## 결정 사항 (이번 개정에서 확정)

- 히어로 헤드라인·서브카피·1차 CTA는 **v0.4 프로토타입 문구를 채택**했다("앞으로 쓸 돈까지 반영해…" / "카드조합 추천받기"). 마스터덱·기존 배포본 문구는 참고용 대안으로만 남긴다.
- 히어로에 특정 혜택 금액(예: "+186,000원")을 **넣지 않기로** 했다 — v0.4 s0 히어로에도 숫자가 없고, 공식 Fixture(186,000원)와 v0.4 자체 예시 숫자(486,000원 등)가 서로 달라 혼선을 피하려는 목적이다.
- 이용 흐름·화면 미리보기 섹션 문구는 v0.4의 각 화면(s0~s7) 문구를 웹 섹션 형태로 옮겼다.

## 미정 목록 (한눈에 보기)

- 히어로 비주얼 종류(현재 초안은 텍스트 카드 2장 — 스크린샷/일러스트로 바꿀지 팀 확인 필요)
- "문제 제기" 섹션을 새로 넣을지 여부
- 화면 미리보기 4종의 비주얼(실제 스크린샷 캡처 여부) — 쓴다면 v0.4 프로토타입이 1차 후보 소스
- 실제 배포 URL
- 푸터 구성 여부·내용
- 랜딩 유입 경로(광고·검색·지인 공유 등) — 현재 자료엔 없음
- **예시 숫자 불일치(보류)** — 히어로에서는 피했지만, 다른 곳에 구체적 혜택 금액을 넣게 되면 v0.4(연 486,000원 등)가 아니라 공식 Fixture(연 186,000원)를 따라야 한다.
- **1단계 플로우 라벨 상충** — v0.4 s1 화면은 "마이데이터 이용 동의하기" 버튼을 쓰지만, `CLAUDE.md`는 실제 동의를 구현하지 않으므로 "예시 데이터 연결하기"로 고정했다. 이번 초안(랜딩 이용 흐름 1단계)은 `CLAUDE.md` 쪽을 따라 "예시 데이터 연결"로 표기했다 — v0.4 프로토타입 원본을 실제로 그렇게 고칠지는 원 작성자 확인 필요.

## 하지 말아야 할 것

- 페르소나를 실제 고객 후기처럼 인용하지 않는다.
- 북극성 40%, 계산오류율 등 내부 검증 지표를 노출하지 않는다.
- 마스터덱에만 있고 앱 계산엔 없는 숫자(시장 규모 1억 7,734만 건, 혼인 24만 건 등)를 고객 대상 가치 제안에 섞지 않는다.
- 문제 제기를 공포 소구로 과장하지 않는다.
