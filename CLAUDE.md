# CardFit — 에이전트 하네스

> 32시간 프로토타입. 결혼·이사처럼 예정된 고액 지출을 앞둔 다장 카드 사용자가, 미래 지출 계획을 기준으로
> 복잡한 카드 조건과 전환비용을 비교하고, 현재 카드를 유지할지 더 유리한 조합으로 바꿀지와 결제 계획을
> 근거와 함께 5분 안에 결정하도록 돕는다.

## 정본 문서

| 무엇 | 어디 |
| --- | --- |
| 범위 잠금 | `docs/SCOPE.md` |
| 제품 목적·사용자·목표 | `docs/PRD.md` |
| 요구사항 (FR·UI·NFR·AC) | `docs/SRS.md` |
| 화면·흐름·상태 | `docs/ux/README.md` |
| **결정 원장** | **`docs/grill/GRILL_LEDGER.md`** |
| 기술·계산·Fixture·테스트 계약 | `docs/specs/` |
| 아키텍처 결정 | `docs/adr/` |
| 사업 모델·전략 (🟡 팀 합의 초안) | `docs/knowledge-base/07-business-model.md` |
| 파일 소유 경계 | `docs/TEAM_OPERATIONS.md` 1절 — **소유 레인만 커밋** |

## 확정된 제약 (되돌리려면 Decision Log에 사유를 남긴다)

- **PRD의 수치는 근거 등급을 함께 유지한다** — 사용자 데이터가 없는 결론 수용률과 실험 임계는 🟡 가정이며 프로토타입 합격 기준으로 쓰지 않는다. 운영형 p95·가용성·오류율을 Mock 프로토타입의 성과처럼 주장하지 않는다.

### UX 계약

- **진입 구조는 단일 선형 흐름이다** (`P04-R1`) — `현재 혜택 요약 → 현재 카드 진단 → 앞으로 쓸 돈 입력 → 변경 조건 → 결론+배분 → 근거 → 확정+경계`. 결론에서 입력으로 되돌아간다.
- **진입 앞에는 온보딩과 마이데이터 Mock 안내를 둔다** (`P04-R2`) — 온보딩은 `카드핏.png`, 모바일은 iPhone 17 402×874 기준이다. 실제 API·동의를 구현하지 않으므로 CTA는 `예시 데이터 연결하기`, 완료 문구는 `예시 데이터 불러오기 완료`로 쓴다.
- **앞단 두 화면은 `BankSalad_Home_0·1`의 정보 위계와 카드 대비 구도만 변환한다.** 두 번째 화면의 Primary CTA는 `앞으로 쓸 돈 반영하기`이고 곧바로 `앞으로 쓸 돈을 알려주세요` 화면으로 이동한다. 기준 선택·과거 순위 대조군·페르소나 선택 화면은 만들지 않는다.
- **미래지출 전 금액은 현재 상태 분석으로만 허용한다** (`S03`) — `최근 12개월 소비 기준`과 `앞으로의 지출은 아직 반영되지 않았어요`를 함께 노출한다. 이 단계에서 유지·정리·신규 판정과 CardFit 최종 조합은 0건이어야 한다.
- **유저플로우의 종착점은 "조합 확정 + 경계 고지"다** (`T25` — 종전 *"결정 + 경계 고지"*를 개정).
  근거 화면의 CTA `이 조합 적용하기`가 확정 액션이고, 화면 5가 확정 결과를 보여준다.
  **고지 문구로 유저플로우를 끝내지 않는다** — 북극성이 *"조합안 저장·확정"*이라 확정 액션이 없으면 지표가 측정 대상을 잃는다.
- **확정한 조합은 확정 시점 값으로 동결하고 자동 재계산하지 않는다** (`T43`) — `rule_version`·기준일·금액을 스냅샷으로 얼린다. `rule_version` 변경 또는 기준일 +30일 경과 시 만료 처리하고, 재진입 시 *"약관이 변경되었을 수 있습니다 · 다시 계산하기"* 안내만 띄운다(사용자 확인 값을 임의 변경하지 않음, `T37` 정합). Mock이라 실제 변경은 없으나 규칙은 유지한다.
- **해지·전환을 대행하지 않는다. 다만 신규 발급 1장은 카드사 공식 페이지로 이동시킨다(아웃링크).** (`T25`)
  아웃링크는 대행이 아니라 이동이다. **해지 항목에는 버튼을 두지 않고 안내 문구만 둔다** — 해지 절차 마찰이 확인된 영역이다.
  - **확정 이후 현실 고지 (`T27`)** — 해지 항목에 *"연회비 환급·포인트 소멸이 있을 수 있으니 카드사에 확인"*, 신규 발급 항목에 *"카드사 심사로 거절될 수 있음"*을 외부화 문구 파일에 둔다. 고지만 하고 대행·정산은 하지 않는다.
  **"현재 조합 유지" 결론에서도 확정 화면을 연다** — 다음 행동이 전부 "계속 사용"이고 아웃링크는 0개다.
- **결과 화면의 주인공은 결제 배분표다** — 조합 결론은 좁은 배너, 배분표가 본문이다. 카드 순위 목록을 만들지 않는다. (`T2`)
- **UX 원칙 정본은 `blueprint_v0.4_prompt.md`, 화면·인터랙션 기준본은 `blueprint_v0.3.html`이다.** 402×874 모바일 프레임을 유지한다. 근거 화면(UI-007)은 결론에서만 진입 가능해야 한다.
- **입력 화면은 빈 폼으로 열리지 않는다** — 과거 패턴 기반 제안값(FR-006)이 이미 채워져 있어야 한다. 제안 확인용 별도 화면을 만들지 않는다. (`T3`) 제안값은 초안이며, 사용자가 **`이 계획대로 계산하기`**를 누르면 수정 여부와 관계없이 화면의 전체 값을 앞으로 12개월의 지출 계획으로 확인한 것으로 본다. 최소 입력 건수·금액 비율은 두지 않는다 (`T37`).
- **"유지" 결론일 때도 배분표를 비우지 않는다** — 현재 조합 기준 배분표 + **"검토했던 대안"**(게이팅 미통과 후보의 혜택·연회비·순손익)을 함께 보여준다. 유지의 설득력은 "안 바꿔도 된다"가 아니라 **"바꾸면 오히려 손해다"**에서 나온다. 추가 계산 없이 `PlanCandidate` 값을 반대로 표시하면 된다. (`T21`)

### 계산·표기

- **금액과 근거 설명 모두 규칙 엔진과 고정 템플릿이 처리한다. 생성형 AI는 런타임 의존성이 아니다** (`C-TEC-005`·`C-TEC-006` 기각 · `SCOPE.md` 5-1절). 금액을 LLM에게 생성시키지 않는다.
- **지출 감소는 항목별 토글로 받는다** — `+ 늘어요` / `− 줄어요`. 금액 칸에 마이너스를 직접 입력받지 않는다. (`T20`)
- **계산 기간은 기준일 + 12개월이고 모든 결과 금액은 "연 n원"이다** (`T15`). 입력이 없는 달은 과거 패턴을 기저로 채운다. 연회비는 안분하지 않는다. 사용자가 확인한 계획을 임의로 증감하지 않는다.
- **결과는 단일 카드 조합안이다.** 카드마다 `신규·유지·정리` 중 하나만 표시한다. 임계 미달이면 모든 보유 카드는 `유지`이며 `신규·정리`는 0건이다.
- **확인할 미래 계획이 0건이면 결과를 반환하지 않는다** (G2 · AC-001). **0건 = 사용자가 모든 항목을 지웠거나 금액이 전부 0인 상태**다. 과거 패턴으로 채운 제안값은 그 자체로 결과가 아니며, 사용자가 `이 계획대로 계산하기`로 확인해야 미래 계획이 된다 (`T19` · `T37`).
- **Error 상태는 구현하지 않는다** — Mock이라 네트워크·인증 오류가 없다. 구현할 화면 상태는 Loading · Empty · 임계 미달 3종이다.
- **탭 네비게이션을 그리지 않는다** — 상단 탭·하단 탭 모두. 화면 하단은 고정 CTA 버튼이 차지한다. 동작하지 않는 가짜 요소를 만들지 않는다. (`T14`)
- **경쟁사 화면에서 레이아웃은 가져오되 금액은 가져오지 않는다** — 큰 숫자 자리에는 관찰된 사실(보유 장수·소비 합계·실적 미달 수)을 넣는다. (`T11`)
- **경쟁 비교는 직접 확인한 범위만 말한다** — 더쎈카드는 확인하지 못했으므로 제외하고 `직접 확인한 5곳 중 0곳`으로만 표현한다. (`P04`)
- **결론 배너는 차액을 손실로 말하고, 캡션이 비교 기준선을 밝힌다** (`T26` · `T22` 흡수) — 기준선 없는 차액을 화면에 띄우지 않는다. 캡션의 카드 장수는 하드코딩하지 않는다.
  - 변경 제안 — 본문 *"지금 조합 그대로면 연 n원을 덜 받습니다"* / 캡션 *"현재 조합 m장을 그대로 쓸 때와 비교 · 확인한 앞으로 12개월 계획 기준"*
  - 현재 조합 유지 — 본문 *"지금은 바꾸지 않아도 돼요 · 바꾸면 연 n원 손해예요"*(`T21`) / 캡션 *"현재 조합과 비교 · 확인한 앞으로 12개월 계획 기준"*
  - **손실의 방향이 결론마다 반대다.** 유지 결론에 *"그대로면 덜 받는다"*를 쓰지 않는다 — G3 위반이다. 변경 제안에 *"바꾸면 손해"*를 쓰지 않는다.
  - **금지어 `총혜택` · `최대혜택` · `놓쳤어요` · `손해보고 있어요`** — 계산하지 않은 금액까지 포함한 것으로 오해되거나 과거 손실을 확정하는 표현이다. FR-008 경계 문구와 같은 파일에 외부화한다.
- **진단 화면에는 금액 결론을 띄우지 않는다** — 절감액·예상혜택·추천 카드·과거 기준 손실 전부 금지. 관찰된 사실만 노출한다. 손실 가시화는 결과 화면의 차액 표시(AC-006)가 담당한다. (`T5`)
- **근거 6항목(실적구간·혜택한도·연회비·제외조건·기준일·미반영 항목) 미달이면 결과를 노출하지 않는다** (AC-002).
- **6항목 미달은 카드 단위로 처리한다** (`T41`) — 미달 카드는 조합 후보 단계에서 제외하고 사유를 근거 화면에 남긴다. 결론 카드는 전원 충족. 미달로 후보가 0이면 "현재 조합 유지"로 폴백하며, 응답 거부는 결론 카드가 미달일 때만이다.
- **기준일이 계산 시점 대비 3개월(팀 상수 🟡) 초과면 경고 고지만 하고 결과를 무효화하지 않는다** (`T41`) — 정직 고지 방식(`T40`과 동일). Mock은 최신 기준일이라 트리거되지 않으며 규칙은 실연동 대비로 둔다.
- **미반영 항목 상한값은 출처와 함께 저장한다** (`T42`) — `unmodeled_bound`에는 **약관에 명시된 한도**를 넣고 `unmodeled_bound_source`(문구·기준일)를 함께 둔다. **출처를 댈 수 없으면 항목을 노출하지 않는다 — `0`으로 두지 않는다.** 없는 것과 0원은 다르다.
- **연회비는 안분하지 않고, 창 후반 발급의 왜곡은 고지로 덮는다** (`T40`) — 발급이 12개월 창의 7개월 이후면 근거 화면에 통째 반영 사실을 1줄 고지한다. 규칙을 바꾸지 않으며 **계산 오류율 판정 대상이 아니다.**
- **근거 화면의 전문 용어를 쉬운 말로 바꾸지 않는다** (`T44`) — 괄호 안에 한 줄 풀이를 첫 등장 1회만 붙인다. 용어를 바꾸면 사용자가 카드사 약관과 대조할 수 없어 차별점 ③이 무너진다.
- **미반영 항목의 금액은 추정하지 않는다** — `BenefitRule.unmodeled_bound`에 선언된 상한만 읽어 "최대 ±n원"으로 표기하고, **결론 차액에 합산하지 않는다**. (`T7`)
- **조합 결론의 사용 카드는 최대 2장, 신규 발급은 최대 1장이다** — 조합 후보를 그 이상 만들지 않는다. 해지는 후보에 남긴다(AC-003 트리거). (`T6`)
- **결과를 "최적 조합"으로 단정하지 않고 "사용 카드 2장·신규 1장 이내에서의 최선"으로 캡션 표기한다** (`T39`) — 상한은 화면 복잡도 때문에 정한 값이라 간극이 있을 수 있다. 간극 탐지를 위해 상한 이상 후보를 계산하지 않으며, 상한 완화 UX는 범위 밖이다.
- **Net Benefit 임계값은 `≥ 연 50,000원` AND `≥ Gross Benefit × 15%`의 이중 조건이다** (`D-002` · `T16`). 둘 중 하나라도 미달이면 **"현재 조합 유지"**를 반환한다 — Error가 아니라 정상 결과다 (AC-004).
- **"현재 조합 유지"를 반환할 때는 원인을 `임계미달` / `제약과다` 2종으로 분해해 `Calculation.hold_reason`에 기록한다** (`T38`). 제약(카드 수·신규 허용)을 완화한 가상 후보가 임계를 넘으면 `제약과다`, 완화해도 못 넘으면 `임계미달`이다. **`제약과다`일 때만** 결과 화면에 *"제약을 풀면 더 나은 조합이 있어요"* 힌트 1줄을 노출한다(Mock 프로필이 이 케이스를 안 만들면 문구만 정의, `T40` 방식). **북극성 선택률 분모는 `임계미달` 유지만 제외하고 `제약과다` 유지는 분모에 포함한다** — 분모 오염 방지.
- **"신청·해지는 카드사에서 직접 진행하셔야 합니다" 경계 문구는 코드에 하드코딩하지 않고 외부화한다** (AC-003).
  금지어 사전(`총혜택` 등)도 같은 파일에 둔다 — 하드코딩하면 금지어 스캔이 불가능해진다.

### 데이터

- **실명 카드를 쓰되 화면이 예시임을 밝힌다** — 조합 리스트 카드 행에 `예시 수치` 배지, 근거 화면 기준일 아래에 "※ 수치는 예시이며 실제 약관과 다를 수 있습니다". Mock 수치를 실명 카드의 실제 조건인 것처럼 단정하지 않는다. (`T18`)
- **Mock 프로필 2개는 내부 테스트 fixture다** — 변경 조합형과 현재 조합 유지형. **사용자 화면에 프로필 선택 UI나 인물 이름을 노출하지 않는다.** 기본 흐름은 변경형 데이터 1세트를 이름 없이 로드하고, 유지형은 테스트에서만 주입한다. (`T17` · `P03`)
- **면책·컴플라이언스 (`T31`)** — 근거/확정 화면에 *"계산 결과는 참고용이며 실제 혜택은 카드사 약관·심사에 따릅니다"* 면책 문구를 외부화 파일에 둔다. **금지어 사전(`총혜택` 등)의 관리 주체는 프로토타입=UX & Build Lead, 실연동=Rule 관리 주체(`T29`)**다. CS가 계산 문의에 답할 때는 `ClientEvent` 로그 + 결정론성(NFR-001)으로 같은 입력을 재현해 검증한다. 규제(마이데이터 사업자 자격·광고성 판단)·카드사 약관 표시 권리는 실연동 범위(`OQ-012`·`OQ-013`).
- **마이데이터는 연동하지 않는다 — 전부 Mock이다** (D-001). 실인증·실결제·민감정보를 다루지 않는다.
- **시작 화면은 동의를 받는 것처럼 그리지 않는다** — 동의할 대상이 없으므로 *"예시 데이터로 동작합니다"* 고지만 한다. 약관·개인정보 체크박스를 만들지 않는다. (`T8`)
- **비밀키·개인정보를 저장소와 번들에 넣지 않는다** (NFR-002).
- **행동 로깅은 `ClientEvent` 6종으로 남긴다** (`T30`) — `입력완료·계산요청·결과열람·근거열람·조합확정·아웃링크클릭`. 북극성(선택률) 측정 근거이며 `조합확정`이 폐기된 OutcomeLog를 대체한다. Mock이라 서버 전송 없이 콘솔/`localStorage`로만 남긴다. 아웃링크 404 감지·발급 Attribution은 실연동 범위다.
- **아웃링크는 새 탭으로 열고, 돌아왔을 때 입력값·확정 조합을 보존한다** (`T28`). 서버에 저장하지 않고 세션 동안의 클라이언트 상태로만 유지한다 — 새로고침 시 홈으로 돌아가는 것은 정상 동작이다. 입력 화면은 재진입 시에도 **직전 입력값이 채워진 채로** 열린다.

### 디자인

- **디자인 토큰은 `docs/ux/README.md` 3절이 정본이다.** 값을 코드에 직접 쓰지 않고 토큰으로 참조한다.
- **다크 영역은 결론 배너(`--banner`) 하나뿐이다** — 다른 화면에 다크를 추가하지 않는다. 늘어나면 결론의 신호값이 사라진다. (`T13`)

### 산출물

- **동작하는 앱이 원본이고 발표 보드는 그 파생물이다** (`T12`). 보드를 먼저 그리지 않는다 — 같은 화면을 두 번 만들게 된다.
- **보드는 앱 화면을 캡처해 조립한다.** 형식 기준은 `docs/knowledge-base/raw/User_Flow.png` — 화면 하단에 `① 이름 / 한 줄 설명 / 화면번호 · FR` 배지.

## 작업 규칙

- 구현이 명세를 조용히 대체하지 않는다. 모호함을 발견하면 `docs/SRS.md`에 되돌려 반영한다.
- 요구사항 없는 화면을 만들지 않는다. 화면은 `FR-0xx` / `UI-0xx`에 연결된다.
- 프로젝트 스킬은 `.claude/README.md` 참고 — `grill-it`·`goal-setting` 각 1회.
