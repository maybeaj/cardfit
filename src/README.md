# src — 프로토타입 소스

> 담당: UX & Build Lead
> **상태: 비어 있음 — Day 2에 스캐폴드**

## 스캐폴드 전에 정할 것

1. 프레임워크와 버전 → `docs/TECH_LEARNING_PLAN.md`
2. 앱 루트 위치 — 저장소 루트 vs `src/` 하위 (`create-next-app` 기본값과 충돌하지 않게)
3. Mock Data 스키마 → `docs/ux/README.md` 4절
4. 디자인 토큰 → `docs/ux/README.md` 3절

## 실행 방법

`README.md` 루트의 "프로토타입 실행 방법"을 채워 주세요.
**다른 팀원이 그 명령만 보고 로컬 실행할 수 있어야** Definition of Done을 통과합니다.

## 규칙

- `src/**` 변경은 **브랜치 + PR 필수** (문서와 달리 main 직접 커밋 금지)
- 공통 기반(토큰·공통 컴포넌트·Mock 스키마)은 **한 사람이 먼저 통합**한 뒤 병렬 구현
- `.env`는 커밋하지 않습니다. `.env.example`만 올립니다
